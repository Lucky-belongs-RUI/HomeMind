# 前端环境配置模板：复制为 .env.local 后按需修改

# 可选：直连后端地址（不配置时默认 /api，走 Vite 代理）
# VITE_API_BASE_URL=http://localhost:8000/api

# 可选：WebSocket 地址（不配置时默认 ws://<hostname>:8000/api/agent/ws）
# VITE_WS_URL=ws://localhost:8000/api/agent/ws
