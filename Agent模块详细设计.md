# Agent 模块详细设计（LangGraph）

> **版本**：2.0（逻辑描述版）
> **定位**：智能家居大模型体验平台 Agent 核心模块设计，基于 LangGraph 状态图实现 RAG 检索、意图识别（含隐式意图）、中断询问、审批控制、工具调用、结果汇总、语言组织、任务执行的完整闭环。
> **核心特性**：
> - RAG 节点检索向量数据库，支持个性化回答与规章制度文件询问
> - 意图识别节点深度理解用户显式与隐式意图（如"我感冒了"→安慰+调温+通风判断+烧热水）
> - LangGraph interrupt 机制实现中断询问与中断审批
> - 权限通过前端按钮选择，所有用户可自主设置高级/普通模式，高级模式自动跳过所有审批
> - 本文档只描述逻辑与内容设计，不含代码实现

---

## 1. 设计概述

### 1.1 设计目标

本 Agent 模块旨在构建一个具备深度理解能力、知识增强、可控可审批的智能家居智能体，核心目标：

1. **RAG 知识增强**：在意图识别前检索向量数据库，获取用户私有数据（个人爱好、生活习惯）和规章制度文件（家庭规则、设备使用规范），为后续意图识别和回复提供上下文支撑。例如用户问"空调温度有什么规定"，RAG 检索规章制度文件，Agent 基于检索结果回答。
2. **深度意图识别**：不仅理解用户字面请求，更挖掘隐式需求。例如用户说"我感冒了"，Agent 需判断出：需要语言安慰、应调高空调温度、判断是否适合开窗通风、可烧热水供用户饮用缓解。
3. **可控工具调用**：所有设备控制操作通过预定义工具执行，工具调用前可触发审批流程，确保安全。
4. **中断澄清能力**：当用户指令不明确或存在歧义时，Agent 主动暂停执行，向用户提问澄清，避免误操作。
5. **前端权限按钮**：权限不绑定用户角色，前端提供权限切换按钮，所有用户（房主/住户/访客）都可自主选择"高级模式"（跳过所有审批）或"普通模式"（需要审批）。高级模式适合熟练用户快速操作，普通模式适合谨慎操作或演示场景。
6. **完整闭环**：从用户输入到 RAG 检索、意图识别、任务规划、审批执行、设备控制、结果汇总、语言回复，形成完整的服务闭环。

### 1.2 核心流程

```text
用户输入 + 权限模式（前端按钮设置）
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│  ① RAG 检索节点（rag_retrieval）★新增                    │
│  根据用户输入检索向量数据库                              │
│  数据源：用户私有数据 + 规章制度文件                     │
│  输出：相关上下文片段，注入后续节点                      │
└────────────────────────┬────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│  ② 意图识别节点（intent_recognition）                    │
│  结合 RAG 上下文，分析显式意图 + 隐式意图                │
│  输出结构化任务列表 + 意图明确度评分                     │
│  例："我感冒了" → [安慰, 调高空调温度, 通风判断, 烧热水]│
└────────────────────────┬────────────────────────────────┘
                         │
                         ▼
              ┌──────────────────────┐
              │ 意图是否明确？       │
              └──────┬───────┬───────┘
                 明确│       │不明确
                     │       ▼
                     │  ┌──────────────────────────────┐
                     │  │  ③ 中断询问节点（clarify）   │
                     │  │  LangGraph interrupt 暂停    │
                     │  │  向用户提问，等待回答        │
                     │  │  用户回答后恢复执行          │
                     │  └──────────┬──────────────────┘
                     │             │（回答后重新识别意图）
                     └─────┬───────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  ④ 任务规划节点（task_planning）                        │
│  将意图映射为具体工具调用任务                            │
│  判断每个任务是否需要工具调用                            │
│  输出：待执行任务列表（含工具名、参数）                  │
└────────────────────────┬────────────────────────────────┘
                         │
                         ▼
              ┌──────────────────────────┐
              │ 权限模式 = 高级？        │
              └──────┬───────────┬───────┘
                 是 │           │否（普通模式）
                     ▼           ▼
        跳过审批     ┌──────────────────────────────┐
        直接执行     │  ⑤ 中断审批节点（approval） │
                     │  LangGraph interrupt 暂停    │
                     │  展示待执行任务，等待用户确认│
                     │  用户勾选确认后恢复执行      │
                     └──────────┬──────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────┐
│  ⑥ 工具调用节点（tool_execution）                        │
│  执行用户确认的任务（设备控制、场景触发等）              │
│  每个任务独立执行，记录成功/失败                         │
│  通过 WebSocket 实时推送设备状态变更                     │
└────────────────────────┬────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│  ⑦ 结果汇总节点（result_aggregation）                    │
│  汇总所有工具执行结果                                    │
│  分类：成功任务、失败任务、跳过任务                      │
│  输出：结构化执行报告                                    │
└────────────────────────┬────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│  ⑧ 组织语言回复节点（response_generation）★核心         │
│  结合 RAG 上下文 + 意图识别 + 执行结果                   │
│  生成自然语言回复，含情感关怀（如感冒了→关怀语）         │
│  对于规章制度询问，基于 RAG 检索结果回答                 │
│  对于个性化问题，结合用户偏好画像回答                    │
└────────────────────────┬────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│  ⑨ 任务执行节点（task_execution）                       │
│  执行非设备控制类任务（如场景联动、定时任务）            │
│  记录操作日志，推送最终结果                              │
└────────────────────────┬────────────────────────────────┘
                         │
                         ▼
                    返回用户回复
                  前端展示 + 语音播报
```

### 1.3 权限模型 ★前端按钮控制

本平台权限不绑定用户角色，而是通过前端按钮由用户自主选择权限模式。设计理念：权限模式是"操作风格"选择，而非"身份等级"划分，所有用户都有权根据场景需要选择。

**权限模式定义**：

| 权限模式 | 别名 | 审批行为 | 中断询问行为 | 适用场景 |
|----------|------|----------|--------------|----------|
| **高级模式** | 快速模式 | 自动跳过所有审批，直接执行 | 意图明确时跳过询问，仅严重歧义才询问 | 熟练用户快速操作、演示流畅场景 |
| **普通模式** | 安全模式 | 所有工具调用需用户审批确认 | 意图不明确时主动询问 | 谨慎操作、新用户体验、重要操作确认 |

**前端权限按钮设计**：

- 位置：AI 助理页面顶部，对话窗口上方
- 形式：开关按钮或单选切换，两个选项"高级模式"/"普通模式"
- 默认值：普通模式（安全优先）
- 切换即时生效：用户切换后，下一次对话即使用新权限模式
- 状态传递：权限模式通过 API 请求参数传递给后端 Agent

**权限模式与用户角色的关系**：

- 权限模式与用户角色（房主/住户/访客）**相互独立**
- 房主可选择普通模式（谨慎操作），访客也可选择高级模式（快速操作）
- 用户角色决定"能否操作某设备"（如访客不能控制设备，即使高级模式也会被工具内权限校验拒绝）
- 权限模式决定"是否需要审批"（高级模式跳过审批，普通模式需要审批）

> **关键区分**：用户角色是"身份权限"（能做什么），权限模式是"操作风格"（怎么做）。两者共同决定最终行为。例如访客选择高级模式，审批会跳过，但工具执行时仍会校验访客无权操作设备，最终被拒绝。

### 1.4 术语表

| 术语 | 含义 |
|------|------|
| RAG | Retrieval-Augmented Generation，检索增强生成，从向量数据库检索相关文档注入 LLM 上下文 |
| 隐式意图 | 用户未直接表达但通过情境可推断的需求，如"我感冒了"隐含需要调高空调温度 |
| LangGraph interrupt | LangGraph 的中断机制，暂停状态图执行，等待外部输入后恢复 |
| 权限模式 | 前端按钮选择的操作风格，高级模式跳过审批，普通模式需要审批 |
| 工具调用 | Agent 通过 Function Calling 调用预定义工具执行设备控制等操作 |
| 线程 | LangGraph 的执行线程，用于中断恢复时定位到正确的执行上下文 |

---

## 2. Agent 状态定义

### 2.1 状态字段说明

Agent 状态在节点间传递，承载整个执行流程的上下文信息。状态字段按职责分为五组：

**用户输入与身份组**：

| 字段 | 类型 | 说明 |
|------|------|------|
| user_input | 字符串 | 用户原始输入文本 |
| user_id | 整数 | 当前用户 ID |
| user_role | 字符串 | 用户角色（owner/resident/guest），用于工具内权限校验 |
| family_id | 整数 | 家庭 ID，用于数据隔离 |
| session_id | 字符串 | 会话 ID，多轮对话标识 |
| permission_mode | 字符串 | 权限模式（high/normal），前端按钮设置，决定是否跳过审批 |

**RAG 检索组**：

| 字段 | 类型 | 说明 |
|------|------|------|
| rag_context | 字符串 | RAG 检索到的相关上下文片段，注入意图识别和回复生成节点 |
| rag_sources | 列表 | RAG 检索来源信息（文件名、片段 ID），用于回复引用标注 |
| is_knowledge_query | 布尔值 | 是否为知识查询类问题（规章制度、个人偏好），决定是否强依赖 RAG |

**意图识别组**：

| 字段 | 类型 | 说明 |
|------|------|------|
| intents | 列表 | 识别出的意图列表，每个意图含类型、描述、明确度 |
| intent_clarity_score | 数值 | 意图明确度评分（0-1），低于阈值触发中断询问 |
| needs_clarification | 布尔值 | 是否需要中断询问用户澄清 |
| clarification_question | 字符串 | 中断询问时向用户展示的问题 |
| user_clarification | 字符串 | 用户对中断询问的回答 |

**任务规划与审批组**：

| 字段 | 类型 | 说明 |
|------|------|------|
| tasks | 列表 | 规划出的任务列表，每个任务含工具名、参数、状态 |
| needs_approval | 布尔值 | 是否需要中断审批 |
| approval_granted | 布尔值 | 用户是否批准执行 |
| approved_task_ids | 列表 | 用户勾选确认执行的任务 ID |

**执行结果与回复组**：

| 字段 | 类型 | 说明 |
|------|------|------|
| tool_results | 列表 | 工具执行结果列表，含成功/失败/跳过状态 |
| success_count | 整数 | 成功执行的任务数 |
| failure_count | 整数 | 失败的任务数 |
| skipped_count | 整数 | 跳过的任务数 |
| final_response | 字符串 | 最终自然语言回复 |
| avatar_emotion | 字符串 | 虚拟形象情感状态（happy/sad/caring/neutral） |

**流程控制组**：

| 字段 | 类型 | 说明 |
|------|------|------|
| messages | 列表 | 对话消息历史（LangGraph 标准消息累加器） |
| iteration | 整数 | 迭代次数，防止死循环 |
| thread_id | 字符串 | LangGraph 线程 ID，中断恢复用 |

### 2.2 状态流转说明

状态在各节点间单向流转，每个节点读取所需字段、处理后写入新字段：

- **RAG 节点**读取 `user_input`，写入 `rag_context`、`rag_sources`、`is_knowledge_query`
- **意图识别节点**读取 `user_input`、`rag_context`，写入 `intents`、`intent_clarity_score`、`needs_clarification`
- **中断询问节点**读取 `needs_clarification`、`clarification_question`，等待 `user_clarification` 后回写
- **任务规划节点**读取 `intents`、`user_clarification`，写入 `tasks`、`needs_approval`
- **中断审批节点**读取 `permission_mode`、`needs_approval`、`tasks`，等待 `approval_granted` 后回写
- **工具调用节点**读取 `tasks`、`approval_granted`、`user_role`、`family_id`，写入 `tool_results`
- **结果汇总节点**读取 `tool_results`，写入 `success_count`、`failure_count`、`skipped_count`
- **组织回复节点**读取 `rag_context`、`intents`、`tool_results`、`user_input`，写入 `final_response`、`avatar_emotion`
- **任务执行节点**读取 `final_response`，执行场景联动等，推送结果

---

## 3. 节点详细设计

### 3.1 RAG 检索节点（rag_retrieval）★新增

#### 3.1.1 节点职责

RAG 检索节点是 Agent 流程的第一个节点，负责从向量数据库检索与用户输入相关的知识上下文，为后续意图识别和回复生成提供信息支撑。

#### 3.1.2 检索数据源

向量数据库包含两类数据，分别存储于不同的集合（Collection）：

**用户私有数据集合**：
- 来源：用户通过"数据上传"页面上传的 PDF/Word 文件
- 内容示例：个人爱好说明（"我喜欢夏天空调 25 度"）、生活习惯（"我通常 23 点睡觉"）、偏好文档
- 检索维度：按 user_id 和 family_id 隔离，仅检索当前用户的私有数据

**规章制度文件集合**：
- 来源：房主上传的家庭规则、设备使用规范、安全制度等文件
- 内容示例：设备使用规定（"空调温度不得低于 16 度"）、安全规范（"离家需关闭所有电器"）、家庭公约
- 检索维度：按 family_id 隔离，家庭所有成员可检索该家庭的规章制度

#### 3.1.3 检索逻辑

节点接收用户输入后，执行以下检索逻辑：

1. **查询向量化**：将用户输入文本通过 embedding 模型转为向量
2. **双集合检索**：分别在用户私有数据集合和规章制度集合中检索 Top-K（默认 3）相关片段
3. **相关性过滤**：过滤掉相似度低于阈值（默认 0.7）的片段，确保检索质量
4. **上下文组装**：将检索到的片段按来源组装，标注来源文件名
5. **知识查询判断**：分析用户输入是否为知识查询类问题（如包含"规定""制度""规则""我喜欢""我的偏好"等关键词），设置 `is_knowledge_query` 标志

#### 3.1.4 检索场景分类

| 用户输入类型 | 检索集合 | 检索目的 | 示例 |
|--------------|----------|----------|------|
| 规章制度询问 | 规章制度集合 | 获取相关规定内容回答 | "空调温度有什么规定？" |
| 个人偏好询问 | 用户私有集合 | 获取用户偏好个性化回答 | "我喜欢什么温度？" |
| 设备控制指令 | 两集合均检索 | 检查是否有相关偏好或规定可参考 | "把空调调到 25 度"（可能检索到"我喜欢 25 度"的偏好） |
| 闲聊/情感表达 | 可选检索 | 辅助理解用户情境 | "我感冒了"（可能检索到健康相关偏好） |
| 知识查询 | 对应集合 | 直接基于检索结果回答 | "公司规定几点下班？" |

#### 3.1.5 输出内容

节点输出以下状态字段：

- `rag_context`：组装后的上下文文本，格式为"[来源1] 内容\n[来源2] 内容..."，若无可写空
- `rag_sources`：来源信息列表，含文件名、片段 ID、相似度分数
- `is_knowledge_query`：布尔值，标识是否为知识查询类问题

#### 3.1.6 与后续节点的协作

- **意图识别节点**：RAG 上下文作为辅助信息注入意图识别 prompt，帮助 LLM 更准确判断意图。例如用户问"空调温度有什么规定"，RAG 检索到规章制度片段，意图识别判断为"查询规章制度"而非"控制设备"
- **组织回复节点**：RAG 上下文用于生成回复内容。对于知识查询类问题，直接基于 RAG 检索结果回答；对于设备控制，可在回复中提及用户偏好（如"已为您调到 25 度，符合您夏季偏好"）

---

### 3.2 意图识别节点（intent_recognition）★核心

#### 3.2.1 节点职责

意图识别节点是 Agent 的"大脑"，负责深度理解用户输入，不仅识别字面请求（显式意图），更挖掘情境隐含需求（隐式意图）。结合 RAG 上下文，输出结构化意图列表和明确度评分。

#### 3.2.2 显式意图与隐式意图

**显式意图**：用户直接表达的请求，如"将卧室空调调到 27 度"→显式意图是设置空调温度。

**隐式意图**：用户未直接表达但通过情境可推断的需求。意图识别节点通过 LLM 深度理解挖掘：

| 用户输入 | 显式意图 | 隐式意图（Agent 自动挖掘） |
|----------|----------|--------------------------|
| "我感冒了" | 无直接请求 | ①语言安慰（情感关怀）②调高空调温度（感冒畏寒）③判断是否适合开窗通风（通风有助恢复但需看天气）④烧热水（饮用热水缓解） |
| "我有点冷" | 无 | ①调高空调温度 ②关闭窗户 ③关闭窗帘（保温） |
| "我要睡觉了" | 无 | ①关灯 ②空调调睡眠模式 ③关窗帘 ④降低音量 |
| "外面好吵" | 无 | ①关窗 ②调低音量 ③关窗帘 |
| "天黑了" | 无 | ①开灯 ②关窗帘 |
| "我回家了" | 无 | ①开客厅灯 ②开空调 ③开窗帘 ④播放欢迎语 |
| "我要出门" | 无 | ①关灯 ②关空调 ③关窗帘 ④锁门提醒 |
| "今天好热" | 无 | ①调低空调温度 ②开窗帘通风判断 ③提醒喝水 |

#### 3.2.3 意图识别逻辑

节点接收 `user_input` 和 `rag_context`，执行以下逻辑：

1. **上下文注入**：将 RAG 检索到的上下文作为辅助信息注入 LLM prompt
2. **LLM 深度分析**：调用 LLM 分析用户输入，识别显式意图和隐式意图
3. **意图结构化**：将识别出的意图转为结构化列表，每个意图含：
   - 类型（device_control/scene_trigger/knowledge_query/emotional_care/clarification）
   - 描述（如"调高卧室空调温度"）
   - 明确度（该意图是否明确，如"调高温度"不明确具体度数）
   - 依赖信息（执行该意图需要哪些信息，如设备名、参数值）
4. **明确度评分**：综合所有意图的明确度，计算整体 `intent_clarity_score`（0-1）
5. **中断判断**：若评分低于阈值（默认 0.6），设置 `needs_clarification = True`，并生成 `clarification_question`

#### 3.2.4 意图识别 prompt 设计要点

意图识别节点的 LLM prompt 需包含以下要素：

- **角色定义**：你是智能家居助理的意图识别引擎，负责深度理解用户输入
- **任务说明**：分析用户的显式意图和隐式意图，输出结构化意图列表
- **显式意图识别规则**：识别用户直接表达的设备控制、场景触发、知识查询等请求
- **隐式意图挖掘规则**：根据用户情境推断潜在需求，如健康问题→调温+通风+热水+安慰
- **RAG 上下文注入区**：注入检索到的用户偏好和规章制度，辅助判断
- **输出格式要求**：JSON 格式，含 intents 列表和 clarity_score
- **示例引导**：提供多个意图识别示例（感冒了、我冷了、空调调温等），引导 LLM 学习隐式意图挖掘

#### 3.2.5 明确度评分规则

意图明确度评分综合考虑以下因素：

- **设备明确度**：用户是否指定了具体设备（如"卧室空调"明确，"空调"不明确）
- **参数明确度**：用户是否指定了具体参数（如"27 度"明确，"调高一点"不明确）
- **房间明确度**：用户是否指定了房间（如"卧室"明确，未提房间不明确）
- **歧义度**：用户输入是否存在多种解读

评分阈值：
- ≥ 0.8：意图非常明确，直接执行
- 0.6-0.8：意图较明确，可执行但可能需审批
- < 0.6：意图不明确，触发中断询问

#### 3.2.6 输出内容

节点输出以下状态字段：

- `intents`：意图列表，每个意图含类型、描述、明确度、依赖信息
- `intent_clarity_score`：整体明确度评分
- `needs_clarification`：是否需要中断询问
- `clarification_question`：中断询问的问题文本（如"请问您想调整哪个房间的空调？当前卧室和客厅都有空调"）

---

### 3.3 中断询问节点（clarification）★中断机制

#### 3.3.1 节点职责

当意图识别节点判断用户指令不明确时，中断询问节点暂停 Agent 执行，向用户提问澄清，等待用户回答后恢复执行。

#### 3.3.2 触发条件

- 意图识别节点输出 `needs_clarification = True`
- 且用户权限模式为任意（高级模式和普通模式都会触发中断询问，但高级模式仅在严重歧义时触发）

**权限模式对中断询问的影响**：

| 权限模式 | 中断询问触发阈值 | 行为 |
|----------|------------------|------|
| 高级模式 | 严格阈值（评分 < 0.4） | 仅严重歧义才询问，轻微不明确自动选择默认值执行 |
| 普通模式 | 宽松阈值（评分 < 0.6） | 较不明确即询问，确保操作准确 |

#### 3.3.3 中断询问流程

1. **暂停执行**：节点调用 LangGraph 的 interrupt 机制，暂停状态图执行
2. **生成问题**：基于 `clarification_question` 向用户展示问题，问题应具体、可回答
3. **等待回答**：Agent 进入等待状态，前端展示问题对话框
4. **用户回答**：用户在前端输入回答，提交后通过 resume API 恢复执行
5. **回答处理**：节点接收用户回答，写入 `user_clarification`
6. **重新识别**：将用户回答与原输入合并，重新触发意图识别节点，更新意图列表

#### 3.3.4 中断询问问题设计

问题应具体、可回答，避免开放性问题。设计原则：

- **提供选项**：如"请问您想调整哪个房间的空调？当前有：①卧室空调 ②客厅空调"
- **明确缺失信息**：如"请问您希望调到多少度？（16-30 度）"
- **确认歧义**：如"您说的'它'是指空调还是灯？"

#### 3.3.5 输出内容

节点输出以下状态字段：

- `user_clarification`：用户回答文本
- 触发意图识别节点重新执行，更新 `intents`

---

### 3.4 任务规划节点（task_planning）

#### 3.4.1 节点职责

任务规划节点将识别出的意图映射为具体的工具调用任务，每个任务对应一个工具调用，含工具名、参数、执行优先级。

#### 3.4.2 意图到工具的映射逻辑

节点维护一个意图-工具映射表，将意图类型映射到具体工具：

| 意图类型 | 映射工具 | 参数来源 |
|----------|----------|----------|
| 设备控制（device_control） | set_device_state | 从意图描述提取房间、设备类型、属性 |
| 设备查询（device_query） | get_device_state / list_room_devices | 从意图描述提取房间、设备类型 |
| 场景触发（scene_trigger） | trigger_scene | 从意图描述提取场景名 |
| 知识查询（knowledge_query） | 无工具，直接用 RAG 上下文回答 | 无 |
| 情感关怀（emotional_care） | 无工具，在回复节点处理 | 无 |

#### 3.4.3 任务规划逻辑

1. **遍历意图**：对每个意图，根据类型匹配对应工具
2. **参数提取**：从意图描述和依赖信息中提取工具所需参数（房间名、设备类型、属性键值）
3. **参数补全**：对于缺失参数，尝试从 RAG 上下文（用户偏好）补全。如用户说"调高空调温度"未指定度数，可从 RAG 检索到的偏好"夏季喜欢 26 度"补全
4. **冲突检测**：检测任务间是否有冲突（如同时调高和调低同一设备温度），有冲突则标记需审批
5. **优先级排序**：按重要性和依赖关系排序任务（如情感关怀优先于设备控制）
6. **审批需求判断**：根据权限模式和任务性质判断是否需要审批

#### 3.4.4 任务结构

每个任务包含以下字段：

- task_id：任务唯一标识
- intent_type：来源意图类型
- tool_name：工具名
- arguments：工具参数（房间名、设备类型、属性等）
- priority：优先级（high/medium/low）
- status：状态（pending/approved/executing/success/failed/skipped）
- needs_approval：该任务是否需要审批

#### 3.4.5 输出内容

节点输出以下状态字段：

- `tasks`：任务列表
- `needs_approval`：是否有任务需要审批（综合判断）

---

### 3.5 中断审批节点（approval）★中断机制+权限跳过

#### 3.5.1 节点职责

中断审批节点在工具调用前，向用户展示待执行任务列表，等待用户勾选确认后恢复执行。**高级权限模式自动跳过此节点**。

#### 3.5.2 权限跳过逻辑 ★核心

节点首先检查 `permission_mode`：

- **高级模式（high）**：直接设置 `approval_granted = True`，所有任务 `status = approved`，跳过中断，直接进入工具调用节点
- **普通模式（normal）**：执行中断审批流程，等待用户确认

这是权限按钮的核心作用点。无论用户角色是房主、住户还是访客，只要选择了高级模式，审批节点都会被跳过。

#### 3.5.3 中断审批流程（普通模式）

1. **暂停执行**：调用 LangGraph interrupt 机制暂停
2. **展示任务**：将待执行任务列表发送给前端，展示任务详情（工具名、参数、预期效果）
3. **等待确认**：用户在前端审批界面勾选要执行的任务，提交确认
4. **恢复执行**：用户提交后通过 resume API 恢复，节点接收用户选择
5. **状态更新**：根据用户勾选，更新任务的 `status`（approved/skipped）

#### 3.5.4 审批界面展示内容

审批界面应展示以下信息，便于用户决策：

- 任务总数和分类（设备控制 X 个、场景触发 Y 个）
- 每个任务的详情：目标设备、操作动作、参数值、预期效果
- 风险提示：对于危险操作（如关闭所有设备、调极端温度）标注警告
- 全选/反选按钮：便于批量操作
- 确认/取消按钮：确认执行勾选任务，取消则终止本次操作

#### 3.5.5 输出内容

节点输出以下状态字段：

- `approval_granted`：是否批准执行
- `approved_task_ids`：用户勾选确认的任务 ID 列表
- 任务 `status` 更新为 approved 或 skipped

---

### 3.6 工具调用节点（tool_execution）

#### 3.6.1 节点职责

工具调用节点执行用户确认（或高级模式自动确认）的任务，调用对应工具完成设备控制、场景触发等操作。

#### 3.6.2 执行逻辑

1. **遍历已批准任务**：仅执行 `status = approved` 的任务
2. **权限校验**：每个工具调用前，校验用户角色是否有权操作目标设备（如访客无权控制设备，即使高级模式也会被拒绝）
3. **参数校验**：校验工具参数合法性（如空调温度 16-30 范围）
4. **执行工具**：调用对应工具函数，修改数据库设备状态
5. **WebSocket 推送**：每次设备状态变更，通过 WebSocket 推送到前端实时更新
6. **记录日志**：记录操作日志到 `device_operation_logs` 表
7. **结果收集**：收集每个任务的执行结果（成功/失败/错误信息）

#### 3.6.3 工具集说明

Agent 可调用的工具集包括：

| 工具名 | 功能 | 参数 | 权限要求 |
|--------|------|------|----------|
| set_device_state | 设置设备状态 | 房间名、设备类型、属性键值 | 房主或住户 |
| get_device_state | 查询设备状态 | 房间名、设备类型 | 所有角色 |
| list_room_devices | 列出房间设备 | 房间名 | 所有角色 |
| trigger_scene | 触发场景 | 场景名 | 房主或住户 |
| query_regulations | 查询规章制度 | 查询关键词 | 所有角色（通过 RAG） |

#### 3.6.4 输出内容

节点输出以下状态字段：

- `tool_results`：工具执行结果列表，每个结果含任务 ID、成功/失败状态、返回数据、错误信息

---

### 3.7 结果汇总节点（result_aggregation）

#### 3.7.1 节点职责

结果汇总节点整理所有工具执行结果，分类统计成功、失败、跳过的任务，生成结构化执行报告，供回复节点使用。

#### 3.7.2 汇总逻辑

1. **遍历 tool_results**：对每个结果按状态分类
2. **统计计数**：计算 `success_count`、`failure_count`、`skipped_count`
3. **错误归因**：对失败任务，分析失败原因（权限不足、参数非法、设备不存在等）
4. **生成报告**：组装结构化执行报告，含统计数据和详细结果

#### 3.7.3 输出内容

节点输出以下状态字段：

- `success_count`、`failure_count`、`skipped_count`
- 结构化执行报告（内含在状态中，供回复节点读取）

---

### 3.8 组织语言回复节点（response_generation）★核心

#### 3.8.1 节点职责

组织语言回复节点结合 RAG 上下文、意图识别结果、工具执行结果，生成自然语言回复。对于知识查询类问题，基于 RAG 检索结果回答；对于设备控制，汇报执行结果并加入情感关怀。

#### 3.8.2 回复生成逻辑

1. **判断回复类型**：根据 `is_knowledge_query` 和 `intents` 判断回复类型
   - 知识查询型：基于 RAG 上下文回答规章制度或个人偏好
   - 设备控制型：汇报执行结果（成功/失败）
   - 情感关怀型：加入关怀语（如感冒了→"要注意休息，多喝热水"）
   - 混合型：综合以上多种元素
2. **RAG 引用**：对于知识查询，在回复中标注来源（如"根据家庭规定，空调温度不得低于 16 度"）
3. **偏好提及**：对于设备控制，若参考了用户偏好，在回复中提及（如"已为您调到 25 度，符合您夏季偏好"）
4. **情感识别**：根据用户输入和意图，设置虚拟形象情感状态（`avatar_emotion`）
5. **失败说明**：对于失败任务，说明原因并给出建议

#### 3.8.3 回复类型与内容设计

**知识查询型回复**（如"空调温度有什么规定？"）：

- 基于 RAG 检索到的规章制度片段回答
- 标注来源文件（如"根据《家庭设备使用规范》"）
- 若无相关规定，说明"未找到相关规定"
- 示例："根据《家庭设备使用规范》，空调温度建议设定在 26-28 度，不得低于 16 度以免过度耗电。"

**设备控制型回复**（如"将卧室空调调到 27 度"）：

- 汇报执行结果（成功/失败）
- 若参考了偏好，提及（如"已调到 27 度，符合您夏季偏好"）
- 若失败，说明原因
- 示例："已将卧室空调调整到 27 度并开启，当前为制冷模式。"

**情感关怀型回复**（如"我感冒了"）：

- 先表达关怀（"听到您感冒了，要注意休息"）
- 汇报已执行的关怀措施（"已为您调高卧室空调温度到 28 度，关闭窗户避免吹风，热水壶已开始加热"）
- 给出建议（"建议多喝热水，注意保暖"）
- 设置虚拟形象为 caring 状态

**混合型回复**：

- 综合以上元素，先关怀后汇报，逻辑清晰

#### 3.8.4 虚拟形象情感映射

根据意图和用户情境，设置虚拟形象情感状态：

| 情境 | avatar_emotion | 形象表现 |
|------|----------------|----------|
| 用户生病/不适（感冒了） | caring | 关怀表情，温和语气 |
| 设备控制成功 | happy | 微笑，满意表情 |
| 设备控制失败 | sad | 遗憾表情，道歉语气 |
| 知识查询回答 | neutral | 专业表情，平稳语气 |
| 用户开心/感谢 | happy | 开心表情，热情语气 |
| 等待用户输入 | idle | 待命表情 |

#### 3.8.5 输出内容

节点输出以下状态字段：

- `final_response`：最终自然语言回复文本
- `avatar_emotion`：虚拟形象情感状态

---

### 3.9 任务执行节点（task_execution）

#### 3.9.1 节点职责

任务执行节点执行非设备控制类任务，如场景联动、定时任务设置、操作日志记录、最终结果推送。

#### 3.9.2 执行内容

1. **场景联动**：若意图涉及场景触发（如"我回家了"→回家模式），执行场景动作
2. **定时任务**：若用户设置定时（如"30 分钟后关空调"），注册定时任务
3. **操作日志**：记录本次 Agent 执行的完整操作日志
4. **结果推送**：通过 WebSocket 推送最终回复和形象状态到前端
5. **对话保存**：保存对话历史到数据库

#### 3.9.3 输出内容

节点完成所有后续处理，Agent 流程结束。

---

## 4. 边与路由设计

### 4.1 状态图流转逻辑

Agent 状态图的边定义了节点间的流转关系，包含顺序边和条件边：

**顺序边（固定流转）**：

- START → RAG 检索节点
- RAG 检索节点 → 意图识别节点
- 任务规划节点 → 中断审批节点（条件：needs_approval）
- 中断审批节点 → 工具调用节点
- 工具调用节点 → 结果汇总节点
- 结果汇总节点 → 组织回复节点
- 组织回复节点 → 任务执行节点
- 任务执行节点 → END

**条件边（动态路由）**：

- 意图识别节点 → 中断询问节点（条件：needs_clarification = True）
- 意图识别节点 → 任务规划节点（条件：needs_clarification = False）
- 中断询问节点 → 意图识别节点（用户回答后重新识别）
- 任务规划节点 → 中断审批节点（条件：needs_approval = True 且 permission_mode = normal）
- 任务规划节点 → 工具调用节点（条件：needs_approval = False 或 permission_mode = high，跳过审批）

### 4.2 路由函数说明

**意图识别后路由**：

- 检查 `needs_clarification`：为 True 路由到中断询问节点，为 False 路由到任务规划节点
- 高级模式下，仅 `intent_clarity_score < 0.4` 时才设置 `needs_clarification = True`

**任务规划后路由（权限跳过核心）**：

- 检查 `permission_mode`：为 high 直接路由到工具调用节点（跳过审批）
- 为 normal 且 `needs_approval = True`：路由到中断审批节点
- 为 normal 且 `needs_approval = False`：路由到工具调用节点

**中断询问后路由**：

- 用户回答后，路由回意图识别节点重新识别意图
- 设置最大重试次数（默认 2 次），超过则终止并提示用户

**中断审批后路由**：

- 用户确认后，路由到工具调用节点
- 用户取消，路由到 END，终止本次操作

### 4.3 流转图

```text
START
  │
  ▼
RAG 检索节点
  │
  ▼
意图识别节点 ──(needs_clarification=True)──→ 中断询问节点
  │                                            │
  │ ←─────────────────────────────────────────┘
  │ (needs_clarification=False)
  ▼
任务规划节点
  │
  ├──(permission_mode=high)──────────────────┐
  │                                          │
  ├──(permission_mode=normal, needs_approval=True)→ 中断审批节点
  │                                            │
  │ ←─────────────────────────────────────────┘
  │ (permission_mode=normal, needs_approval=False)
  ▼
工具调用节点
  │
  ▼
结果汇总节点
  │
  ▼
组织回复节点
  │
  ▼
任务执行节点
  │
  ▼
END
```

---

## 5. 中断机制设计

### 5.1 LangGraph interrupt 机制说明

LangGraph 的 interrupt 机制允许状态图在执行过程中暂停，等待外部输入后恢复。机制要点：

- **暂停**：节点调用 interrupt 函数，状态图暂停，当前状态持久化
- **线程 ID**：每次中断生成唯一线程 ID，用于恢复时定位执行上下文
- **恢复**：外部通过 resume API 传入用户输入，状态图从暂停点恢复执行
- **状态保持**：中断期间所有状态字段保持不变，恢复后继续处理

### 5.2 中断询问流程

**触发场景**：意图识别节点判断用户指令不明确（`needs_clarification = True`）

**流程**：

1. 意图识别节点生成 `clarification_question`（如"请问您想调整哪个房间的空调？"）
2. 中断询问节点调用 interrupt，暂停执行，返回中断信息给前端
3. 前端展示问题对话框，用户输入回答
4. 用户提交回答，前端调用 resume API，传入回答内容
5. 后端通过线程 ID 恢复执行，中断询问节点接收回答，写入 `user_clarification`
6. 路由回意图识别节点，结合用户回答重新识别意图
7. 重新识别后继续后续流程

**中断询问信息格式**：

- type：clarification
- question：问题文本
- options：可选选项列表（如多个房间名）
- thread_id：线程 ID，用于恢复

### 5.3 中断审批流程

**触发场景**：任务规划节点输出 `needs_approval = True`，且 `permission_mode = normal`

**流程**：

1. 任务规划节点输出待执行任务列表
2. 中断审批节点检查权限模式，高级模式直接跳过（`approval_granted = True`）
3. 普通模式调用 interrupt，暂停执行，返回任务列表给前端
4. 前端展示审批界面，用户勾选要执行的任务
5. 用户提交确认，前端调用 resume API，传入勾选的任务 ID
6. 后端恢复执行，中断审批节点接收用户选择，更新任务状态
7. 路由到工具调用节点，仅执行用户勾选的任务

**中断审批信息格式**：

- type：approval
- tasks：待执行任务列表（含工具名、参数、预期效果）
- thread_id：线程 ID
- risk_level：整体风险等级（low/medium/high）

### 5.4 权限跳过逻辑 ★核心

权限跳过是本设计的关键特性，通过前端按钮控制，所有用户可自主设置：

**权限模式传递**：

- 前端 AI 助理页面有权限切换按钮（高级模式/普通模式）
- 用户切换后，权限模式保存在前端状态中
- 每次 Agent 对话请求，权限模式作为参数传递给后端
- 后端将权限模式写入 AgentState 的 `permission_mode` 字段

**跳过逻辑作用点**：

中断审批节点是权限跳过的主要作用点：

- 节点入口检查 `permission_mode`
- 高级模式：直接设置 `approval_granted = True`，所有任务标记为 approved，不调用 interrupt，直接路由到工具调用节点
- 普通模式：执行正常中断审批流程

**中断询问的权限影响**：

中断询问节点也受权限模式影响，但程度较轻：

- 高级模式：仅严重歧义（评分 < 0.4）才触发中断询问，轻微不明确自动选择默认值
- 普通模式：较不明确（评分 < 0.6）即触发中断询问

**权限模式与用户角色的协同**：

- 权限模式决定"是否审批"（流程层面）
- 用户角色决定"能否操作"（工具执行层面）
- 两者独立运作，共同决定最终行为
- 示例：访客选择高级模式，审批跳过，但工具执行时校验访客无权控制设备，最终被拒绝，回复"您是访客，无权操作设备"

### 5.5 API 层中断处理

Agent 对话 API 需要支持中断恢复，接口设计：

**首次对话接口**：

- 路径：POST /api/agent/chat
- 请求参数：message、session_id、permission_mode（前端按钮设置）
- 响应：正常完成返回 final_response；中断返回 interrupt 信息（type、question/tasks、thread_id）

**中断恢复接口**：

- 路径：POST /api/agent/resume
- 请求参数：thread_id、resume_data（用户回答或审批决策）
- 响应：正常完成返回 final_response；再次中断返回新的 interrupt 信息

**权限模式参数**：

- permission_mode 为字符串，值为 "high" 或 "normal"
- 前端每次请求都传递当前权限模式
- 后端写入 AgentState，影响审批和询问逻辑

---

## 6. 业务场景示例

### 6.1 场景一："我感冒了"完整链路（高级模式）★核心

**用户输入**："我感冒了"
**权限模式**：高级模式（前端按钮设置）
**用户角色**：房主

**执行流程**：

1. **RAG 检索节点**：
   - 检索用户私有数据：找到"用户夏季偏好空调 26 度""用户有热水壶"等片段
   - 检索规章制度：找到"感冒期间建议调高温度至 28 度"的健康提示
   - 输出 rag_context，is_knowledge_query = False

2. **意图识别节点**：
   - 显式意图：无直接请求
   - 隐式意图挖掘：
     - ①情感关怀（用户生病需要安慰）
     - ②调高卧室空调温度（感冒畏寒，参考 RAG 建议调到 28 度）
     - ③判断通风（开窗有助恢复但需看天气，当前假设天气良好可短暂通风）
     - ④烧热水（饮用热水缓解症状）
   - intent_clarity_score = 0.85（意图明确，情境清晰）
   - needs_clarification = False

3. **任务规划节点**：
   - 任务1：set_device_state（卧室空调，温度 28 度，模式制热）
   - 任务2：set_device_state（卧室窗户，短暂开启通风 10 分钟后关闭）—— 若有智能窗
   - 任务3：set_device_state（热水壶，开启加热）—— 若有智能热水壶
   - 任务4：情感关怀回复（在回复节点处理，无工具调用）
   - needs_approval = True（有多个设备控制任务）

4. **中断审批节点**：
   - 检查 permission_mode = high → **跳过审批**
   - 所有任务 status = approved
   - 直接进入工具调用节点

5. **工具调用节点**：
   - 执行任务1：卧室空调调到 28 度制热模式 → 成功，WebSocket 推送
   - 执行任务2：卧室窗户开启 → 成功（假设有智能窗）
   - 执行任务3：热水壶加热 → 成功（假设有智能热水壶）
   - 记录操作日志

6. **结果汇总节点**：
   - success_count = 3，failure_count = 0，skipped_count = 0

7. **组织回复节点**：
   - 回复类型：情感关怀型 + 设备控制型
   - 回复内容："听到您感冒了，要注意休息多喝热水。已为您将卧室空调调到 28 度制热模式，打开了卧室窗户短暂通风（10 分钟后自动关闭），热水壶已开始加热。建议您多喝温水，注意保暖，祝早日康复。"
   - avatar_emotion = caring（关怀表情）

8. **任务执行节点**：
   - 注册定时任务：10 分钟后关闭卧室窗户
   - 保存对话历史
   - WebSocket 推送回复和形象状态

**前端展示**：
- 对话窗口显示关怀回复
- 虚拟形象切换为 caring 表情
- 设备卡片实时更新（空调 28 度、窗户开启、热水壶加热中）
- 语音播报回复内容

### 6.2 场景二："将卧室空调调到 27 度"简单链路（高级模式）

**用户输入**："将卧室空调调到 27 度"
**权限模式**：高级模式
**用户角色**：房主

**执行流程**：

1. **RAG 检索节点**：
   - 检索用户私有数据：可能找到"用户夏季偏好 26 度"（与 27 度接近）
   - is_knowledge_query = False

2. **意图识别节点**：
   - 显式意图：设置卧室空调温度 27 度
   - 隐式意图：无（指令已非常明确）
   - intent_clarity_score = 0.95
   - needs_clarification = False

3. **任务规划节点**：
   - 任务1：set_device_state（卧室空调，温度 27 度，自动开启 power=on）
   - needs_approval = True

4. **中断审批节点**：
   - permission_mode = high → **跳过审批**

5. **工具调用节点**：
   - 执行任务1：卧室空调调到 27 度 → 成功

6. **结果汇总节点**：success_count = 1

7. **组织回复节点**：
   - 回复："已将卧室空调调整到 27 度并开启，当前为制冷模式。"
   - avatar_emotion = happy

8. **任务执行节点**：保存对话，推送结果

### 6.3 场景三：中断询问（意图不明确）

**用户输入**："帮我调一下"
**权限模式**：普通模式
**用户角色**：住户

**执行流程**：

1. **RAG 检索节点**：无相关检索结果

2. **意图识别节点**：
   - 显式意图：调整某设备（但未指定设备和参数）
   - 隐式意图：无
   - intent_clarity_score = 0.3（严重不明确）
   - needs_clarification = True
   - clarification_question = "请问您想调整哪个设备？当前家庭有：①卧室空调 ②客厅空调 ③客厅灯 ④卧室灯 ⑤客厅窗帘。请选择或说明具体需求。"

3. **中断询问节点**：
   - 调用 interrupt，暂停执行
   - 返回中断信息给前端

**前端展示**：
- 对话窗口显示问题："请问您想调整哪个设备？"
- 提供选项按钮供用户选择

**用户回答**："卧室空调，调到 26 度"

4. **中断恢复**：
   - 用户提交回答，resume API 恢复执行
   - user_clarification = "卧室空调，调到 26 度"

5. **重新意图识别**：
   - 结合用户回答，意图明确：设置卧室空调 26 度
   - intent_clarity_score = 0.95
   - needs_clarification = False

6. **任务规划节点**：
   - 任务1：set_device_state（卧室空调，温度 26 度）
   - needs_approval = True

7. **中断审批节点**：
   - permission_mode = normal → 执行审批流程
   - 调用 interrupt，展示任务列表

**前端展示审批界面**：
- 待执行任务：设置卧室空调温度为 26 度
- 用户勾选确认

8. **用户确认后**：
   - 恢复执行，工具调用节点执行任务
   - 成功后回复"已将卧室空调调整到 26 度并开启。"

### 6.4 场景四：中断审批（普通模式）

**用户输入**："关闭客厅所有设备"
**权限模式**：普通模式
**用户角色**：房主

**执行流程**：

1. **RAG 检索节点**：检索到规章制度"离家前关闭所有电器"

2. **意图识别节点**：
   - 显式意图：关闭客厅所有设备
   - 隐式意图：可能用户要出门（可提醒锁门）
   - intent_clarity_score = 0.8
   - needs_clarification = False

3. **任务规划节点**：
   - 任务1：set_device_state（客厅空调，power=off）
   - 任务2：set_device_state（客厅灯，power=off）
   - 任务3：set_device_state（客厅窗帘，position=0）
   - 任务4：set_device_state（客厅电视，power=off）—— 若有
   - needs_approval = True（批量关闭属较重要操作）

4. **中断审批节点**：
   - permission_mode = normal → 执行审批
   - 调用 interrupt，展示 4 个待执行任务
   - risk_level = medium（批量操作）

**前端审批界面**：
- 展示 4 个任务详情
- 用户可勾选全部或部分
- 用户勾选全部，点击确认

5. **用户确认后**：
   - 恢复执行，工具调用节点执行 4 个任务
   - 全部成功

6. **组织回复节点**：
   - 回复："已为您关闭客厅所有设备（空调、灯、窗帘、电视）。检测到您可能要出门，记得锁门哦。"
   - avatar_emotion = happy

### 6.5 场景五：规章制度询问（RAG 知识查询）

**用户输入**："空调温度有什么规定？"
**权限模式**：任意
**用户角色**：住户

**执行流程**：

1. **RAG 检索节点**：
   - 检索规章制度集合：找到"空调温度建议 26-28 度，不得低于 16 度"片段
   - 来源：《家庭设备使用规范》
   - is_knowledge_query = True

2. **意图识别节点**：
   - 显式意图：查询空调温度规定
   - 隐式意图：无
   - 意图类型：knowledge_query
   - intent_clarity_score = 0.95

3. **任务规划节点**：
   - 无工具调用任务（知识查询直接用 RAG 上下文回答）
   - needs_approval = False

4. **中断审批节点**：跳过（无任务需审批）

5. **工具调用节点**：跳过（无任务）

6. **结果汇总节点**：无工具结果

7. **组织回复节点**：
   - 回复类型：知识查询型
   - 基于 RAG 上下文回答
   - 回复："根据《家庭设备使用规范》，空调温度建议设定在 26-28 度，不得低于 16 度以免过度耗电。夏季建议 26 度制冷，冬季建议 20 度制热。"
   - avatar_emotion = neutral

8. **任务执行节点**：保存对话，推送结果

### 6.6 场景六：高级模式 vs 普通模式对比

**相同输入**："关闭所有设备"
**用户角色**：房主

**高级模式执行**：

1. 意图识别：关闭所有设备，明确度 0.8
2. 任务规划：生成关闭所有设备的任务列表
3. 中断审批节点：permission_mode = high → **跳过审批**
4. 工具调用：直接执行所有任务
5. 回复："已为您关闭所有设备。"

**普通模式执行**：

1. 意图识别：关闭所有设备，明确度 0.8
2. 任务规划：生成关闭所有设备的任务列表
3. 中断审批节点：permission_mode = normal → **执行审批**
4. 调用 interrupt，展示所有待关闭设备列表
5. 用户勾选确认（或取消部分）
6. 工具调用：仅执行用户勾选的任务
7. 回复："已为您关闭确认的设备。"

**对比结论**：高级模式快速直接，适合熟练用户；普通模式安全可控，适合谨慎操作。

---

## 7. 前端交互设计

### 7.1 权限按钮设计 ★核心

**位置**：AI 助理页面顶部，对话窗口上方

**形式**：开关切换按钮，两个选项

- 高级模式（别名"快速模式"）：跳过所有审批，直接执行
- 普通模式（别名"安全模式"）：需要审批确认

**默认值**：普通模式（安全优先）

**交互行为**：

- 用户点击切换按钮，权限模式即时切换
- 切换后显示提示"已切换到 XX 模式"
- 下一次对话即使用新权限模式
- 权限模式状态保存在前端 store 中

**视觉设计**：

- 高级模式：按钮高亮，标注"快速执行，跳过审批"
- 普通模式：按钮默认，标注"安全执行，需审批"

### 7.2 中断询问 UI

**触发**：Agent 返回 interrupt_type = clarification

**界面元素**：

- 问题文本：展示 Agent 的问题
- 选项按钮：若问题含选项，展示可点击的选项按钮
- 文本输入框：供用户自由输入回答
- 提交按钮：提交回答，恢复 Agent 执行

**交互流程**：

1. Agent 中断，前端收到问题
2. 展示问题对话框，禁用普通输入
3. 用户选择选项或输入回答
4. 点击提交，调用 resume API
5. 等待 Agent 恢复执行，展示后续回复

### 7.3 中断审批 UI

**触发**：Agent 返回 interrupt_type = approval

**界面元素**：

- 任务列表表格：展示每个待执行任务
  - 任务描述（如"设置卧室空调温度为 27 度"）
  - 目标设备（如"卧室空调"）
  - 操作动作（如"温度→27，电源→开"）
  - 预期效果（如"空调将开启并调到 27 度"）
  - 风险等级标注（低/中/高）
- 复选框：每个任务前有复选框，供用户勾选
- 全选/反选按钮：批量操作
- 确认按钮：确认执行勾选的任务
- 取消按钮：取消本次操作

**交互流程**：

1. Agent 中断，前端收到任务列表
2. 展示审批对话框，列出所有任务
3. 用户勾选要执行的任务（默认全选）
4. 点击确认，调用 resume API 传入勾选任务 ID
5. 等待 Agent 执行，展示执行结果

### 7.4 权限模式与中断的关系

**高级模式下的中断**：

- 中断审批：完全跳过，不展示审批界面
- 中断询问：仅严重歧义才触发，大部分情况直接执行

**普通模式下的中断**：

- 中断审批：所有需审批的任务都触发，展示审批界面
- 中断询问：较不明确即触发，展示询问界面

**前端处理逻辑**：

- 收到 Agent 响应后，检查 interrupt_type
- 若为 approval 且当前为高级模式：理论上不应出现（后端已跳过），若出现则提示异常
- 若为 approval 且当前为普通模式：展示审批界面
- 若为 clarification：展示询问界面
- 若无 interrupt：展示最终回复

---

## 8. 设计总结

### 8.1 核心设计要点

1. **九节点完整闭环**：RAG 检索 → 意图识别 → 中断询问 → 任务规划 → 中断审批 → 工具调用 → 结果汇总 → 组织回复 → 任务执行
2. **RAG 知识增强**：新增 RAG 检索节点，支持个性化回答和规章制度文件询问，检索用户私有数据和规章制度两个集合
3. **双中断机制**：中断询问（意图不明确）+ 中断审批（工具调用前确认）
4. **前端权限按钮**：权限不绑定角色，所有用户可自主选择高级/普通模式，高级模式跳过所有审批
5. **隐式意图挖掘**：通过 LLM 深度理解，如"感冒了"→调温+通风+热水+安慰
6. **情感关怀回复**：回复节点根据情境生成关怀语，设置虚拟形象情感状态
7. **权限模式与角色独立**：权限模式决定"是否审批"，用户角色决定"能否操作"，两者协同

### 8.2 与原 Agent 设计的差异

| 维度 | 原 Agent 设计 | 本设计（增强版） |
|------|---------------|------------------|
| 节点数 | 4（rag→kg→agent→tools） | 9（完整闭环） |
| RAG 节点 | 作为上下文注入 | 独立节点，双集合检索（私有+规章制度） |
| 意图识别 | 无独立节点 | 独立节点，含隐式意图挖掘 |
| 中断机制 | 无 | 双中断（询问+审批） |
| 权限控制 | 工具内角色校验 | 前端按钮权限模式 + 工具内角色校验，独立协同 |
| 回复生成 | Agent 节点兼任 | 独立节点，含情感关怀和 RAG 引用 |
| 任务规划 | 无 | 独立节点，意图→工具映射 |
| 知识查询 | 无专门处理 | RAG 节点 + 回复节点协同，支持规章制度询问 |

### 8.3 扩展性

- **新增工具**：在任务规划节点的意图-工具映射表中注册新工具
- **新增意图类型**：在意图识别节点的 prompt 中补充示例和规则
- **新增 RAG 数据源**：在 RAG 检索节点新增集合（如设备手册、常见问题）
- **新增审批规则**：在中断审批节点扩展风险等级和审批策略
- **权限模式扩展**：可新增"自定义模式"，用户勾选哪些操作需审批
- **情感识别增强**：在组织回复节点接入情感分析模型，更精准识别用户情绪

### 8.4 关键设计决策说明

**为什么 RAG 节点在意图识别之前**：

RAG 检索到的上下文能辅助意图识别。例如用户问"空调温度有什么规定"，若先意图识别可能误判为"控制空调温度"，但 RAG 先检索到规章制度片段，意图识别可正确判断为"查询规章制度"。

**为什么权限不绑定用户角色**：

用户角色是"身份权限"（能做什么），由数据安全和业务逻辑决定，不应随意改变。权限模式是"操作风格"（怎么做），是用户体验选择，应允许用户根据场景灵活调整。例如房主在演示时可能希望走审批流程展示交互，在个人使用时希望快速执行。两者分离设计，既保证安全又提供灵活性。

**为什么高级模式仍保留工具内角色校验**：

权限模式跳过的是"审批流程"（是否需要用户确认），而非"权限校验"（是否有权操作）。访客选择高级模式，虽然跳过审批，但本质上无权控制设备，工具执行时仍会被角色权限校验拒绝。这确保了权限模式不会成为安全漏洞。
